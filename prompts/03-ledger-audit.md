# Ledger audit (monthly)

Drag every file in `10-Literature/` into the conversation, then paste this.

---

These are all my paper notes. The frontmatter in each file holds
structured fields.

1. Build a topic × method cross-tab and show the count in each cell.

2. Flag the empty cells and the cells with only one paper.
   Distinguish two cases, and say when you can't tell:
   - genuinely nobody has done this combination
   - I just haven't read that literature yet

3. List the papers where `gap` is non-empty and `rating >= 4`.
   Cluster them by how similar the gaps are, and give each cluster
   a one-line label.

4. Find every paper with `pub_status: wp` whose `draft_date` is more
   than 18 months old. I need to check whether these were published,
   revised, or withdrawn.

5. Name the blind spots in what I read: which method I over-rely on,
   which kind of evidence I have almost none of.

No generic advice like "read more widely." Name specific cells and
specific papers.
